# 快捷宠 0.11.10 验收报告

验收环境：Windows 双显示器，主屏 1920x1080 / 100%，副屏 1536x960 / 125%。

## 已自动验收

- `npm test`：74 项全部通过。
- 真实双屏拖动：从 100% 主屏拖到 125% 副屏，最终位置误差 1 px，窗口尺寸误差 1 px，无报错。
- 桌宠窗口：创建、显示、直接拖动、自动散步、边缘范围和透明区域点击穿透通过。
- 3D：内置狐狸和模型库导入副本均能显示颜色，两个采样帧动作不同，资源释放通过。
- 2D 图片：本地 Worker 自动抠图通过，前景比例 0.848。
- 性能：平均 CPU 0.74%，峰值内存 355.4 MB，空闲 3D 平均 30.06 FPS。
- 主面板：窗口居中，设置、模型中心、自动化中心和全局搜索无裁切或重叠。
- Playwright：添加快捷方式、分类、组合键、标签、收藏、主题、跨屏设置、规则、提醒、月历和搜索键盘路径通过；控制台 0 错误、0 警告。
- Electron IPC：状态、分类、快捷方式、排序、图标、可用性检查、扫描导入、设置、互动、规则、提醒、动态文件夹、多桌宠、剪贴板、模型读取、备份、存储、缓存、更新、命令和通知共 19 个功能域通过。
- 本机扫描：桌面 82 项、开始菜单 321 项、Chrome/Edge 书签 559 项；只读取候选，没有导入。
- 数据安全：图片文件化、备份恢复、完整迁移、损坏恢复、更新 SHA-256、路径边界和大文件限制通过。
- 便携逻辑：EXE 同目录缓存、当前缓存保护、旧缓存清理、权限错误区分和开机启动目标通过。

## 需要人工验收

这些项目会修改 Windows 状态、打开用户文件或依赖用户提供的素材，自动测试没有代替你确认。

1. 外部目标打开
   - 添加一个真实网址、程序、文件和文件夹，各点击一次。
   - 预期：由对应浏览器、程序或资源管理器打开，使用次数增加。

2. 文件拖放与系统文件选择框
   - 把网址、程序、文件和文件夹分别拖入面板，再使用“浏览”添加一次。
   - 预期：弹出预览或新增记录，不重复添加同一目标。

3. 用户皮肤
   - 分别选择 JPG/PNG、透明 PNG、WebP 和 GIF。
   - 预期：照片自动抠图；透明图保持透明；GIF 保持动画且不抠图；“使用原图/重新抠图/恢复默认”均有效。

4. 用户模型与 Live2D
   - 导入你自己的 GLB、VRM、model3.json，并逐项切换动作、缩放、旋转、翻转和高度。
   - 预期：内嵌贴图正常；缺文件会在体检中指出；Live2D 动作、表情、物理和视线追踪可见。

5. 全局快捷键
   - 切到其他程序，按全局搜索键和一个快捷项目的自定义组合键。
   - 预期：搜索框置前；项目直接打开；冲突组合键显示不可注册。

6. 托盘与桌宠菜单
   - 左击托盘，右击托盘，普通右击桌宠，Shift+右击桌宠。
   - 预期：面板切换、托盘菜单、圆盘菜单和传统菜单均可操作，退出能完全结束进程。

7. 全屏自动隐藏
   - 在两块屏幕上分别打开真正的全屏视频或游戏，再退出全屏。
   - 预期：桌宠进入全屏后隐藏，退出后自动回来，另一块屏幕不应误触发。

8. Windows 通知与到时提醒
   - 新建一个两分钟后的提醒，保持程序后台运行。
   - 预期：到时出现 Windows 通知和应用内通知，可完成或延后 10 分钟。

9. 剪贴板气泡
   - 复制一个网址和一个存在的本地路径。
   - 预期：主桌宠出现收纳气泡；收纳后新增，忽略后同一内容不反复提示。

10. 开机启动
    - 开启开机启动，注销或重启 Windows。
    - 预期：登录后自动出现桌宠；关闭后下次不再启动。

11. 安装、卸载与管理员清理
    - 运行安装版，检查桌面/开始菜单快捷方式；测试保留个人数据和同时删除个人数据两种移除方式。
    - 预期：安装目录、快捷方式和数据处理符合选择；受保护旧缓存会出现 UAC 请求。

12. 在线更新
    - 配置正式 HTTPS 更新清单后检查并下载一次。
    - 预期：只接受 HTTPS 和正确 SHA-256；当前项目尚未配置公开更新源。

## 已知外部限制

- Windows EXE 未签名，首次运行可能出现 SmartScreen“未知发布者”。
- 当前没有随项目分发的完整 Live2D 示例，因此只能自动验证导入、文件引用、体积限制和运行时装载代码，实际画面需要用户模型。
- 本次只覆盖当前横向双屏 100%/125% 组合；旋转屏、上下排列和三屏需要对应硬件人工复测。
