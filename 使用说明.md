# 快捷宠使用说明

## 自动化中心

从左下角进入“自动化中心”，可设置智能分类规则、剪贴板收纳、动态文件夹、提醒通知和多桌宠。复制网址或本地路径后，点击主桌宠气泡中的“收纳”即可保存。普通右键打开圆盘菜单，Shift + 右键打开传统菜单。

模型仓库支持 `.glb`、`.vrm` 和 Live2D 的 `.model.json` / `.model3.json`。Live2D 支持动作、表情、物理、眨眼和鼠标视线追踪，所有模型文件只在本机读取。

## 最简单的安装方法

双击 `dist` 文件夹中的 `QuickPet-Setup-0.11.9-x64.exe`。它会自动安装到当前账户、创建桌面和开始菜单快捷方式并启动快捷宠，以后直接点击快捷方式即可，不需要重复解压。

如果 Windows 显示“未知发布者”或 SmartScreen 提示，这是因为当前测试版还没有购买商业代码签名证书。确认文件来自本项目后，可选择“更多信息”→“仍要运行”。

## 不想安装

直接运行 `QuickPet-Portable-0.11.9-x64.exe`。它不会安装或创建快捷方式，首次启动会在 EXE 旁创建 `QuickPet-Portable-Cache` 运行目录，以后继续双击同一个 EXE 即可，不需要手动解压。发现旧版本缓存时会询问是否清理，也可以在“设置 - 存储与卸载”中关闭提醒或手动清理。

## 怎么操作

1. 桌宠出现后，直接按住桌宠模型拖动即可移动位置。
2. 单击桌宠，打开快捷收纳面板。
3. 点击“添加”输入网址，或通过“浏览”选择程序、文件和文件夹。
4. 也可以把网址、程序、文件或文件夹直接拖进面板。
5. 单击快捷卡片即可打开对应内容。
6. 在“设置与外观”中更换桌宠图片、主题、透明度和分类。

编辑快捷项目时，点击“快捷启动组合键”输入框并按下组合键即可录制。组合键至少需要一个 `Ctrl`、`Alt`、`Shift` 或 `Win` 修饰键；与全局搜索或其他项目重复时不会保存。

需要自己排序时，把排序方式切换为“手动排序”，直接拖动卡片即可；拖到左侧分类名称上可以跨分类移动。分类管理中可以选择上级分类，建立多级目录。

桌宠会自动左右散步、停留、转身和偶尔跳跃。打开面板时它会自动停下，手动拖动后也会暂停几秒。在设置页面可以关闭“自动散步”或调节行走速度。

在“设置与外观”的“桌宠模式”中，可选择：

- `2D 自定义皮肤`：使用默认图片或你上传并自动抠图的皮肤。
- `3D 动态桌宠`：可选择内置狐狸或保存多只自定义 GLB。旧版 GLB 白模会自动恢复内嵌颜色贴图；每只模型都能单独调整大小、旋转、翻转、上下位置和动作映射。

设置页还可以扫描桌面、开始菜单和浏览器书签；预览勾选后再批量导入。默认按 `Alt + Space` 可在任何窗口呼出快捷搜索，如果系统占用了该组合键，会自动改为 `Ctrl + Alt + Space`。性能模式默认使用“无感模式”，待机时自动降到低帧率，互动和走路时才临时提速。

全局搜索中输入 `>` 可以执行打开设置、检查快捷方式、保存面板截图、清理缓存和检查更新等命令。设置页的“快捷键台”集中显示每个项目的组合键与注册状态，“本地使用统计”可以查看高频项目或随时清零。

“换机迁移”会把快捷方式、分类、设置和模型合并成 `.quickpet` 文件；另一台电脑导入后即可恢复。“存储与卸载”可以查看数据、模型、备份和便携缓存占用，移除程序时可选择是否同时删除个人数据。

如果上次运行异常退出，快捷宠下次会自动使用 2D 无感安全模式。确认模型没有问题后，在设置页点击“恢复原配置”。模型导入体检会显示 A-D 性能等级，高负载模型建议继续使用无感模式。

上传 PNG、WebP、JPG 照片后会默认在本机自动抠图，不会把照片上传到服务器。如果效果不满意，可以点击“使用原图”或“重新抠图”。GIF 动图为了保留动画，暂时不自动抠图。

关闭面板不会退出快捷宠。需要完全退出时，请右键桌宠，或在任务栏右下角托盘中选择“退出”。

## 数据与备份

快捷方式和设置保存在本机。程序每天自动备份并保留最近 10 份，配置损坏时会自动恢复；也可以在设置中立即备份、恢复指定版本或导出到文件。更换电脑后，使用“从文件恢复”即可。

## 提醒月历

在“自动化中心”可按月查看提醒。日期右下角数字表示当天提醒数量；点击日期会自动填入时间。提醒可以按今天、明天、本周筛选，也可以标记完成或延后 10 分钟。
